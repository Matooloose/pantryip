# Required
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional - for Whisper audio transcription (Web Speech API is used by default in browser)
OPENAI_API_KEY=your_openai_api_key_here

# Optional - for price caching (app works without it, just slower)
REDIS_URL=redis://localhost:6379
